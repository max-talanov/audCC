# Python dependencies for the auditory thalamo-cortical sleep model.
#
#   pip install -r requirements.txt
#
# Core analysis/plotting libraries used across the code (tc_run.py,
# tc_validate.py, tc_analyze.py, tc_present.py, tc_architecture.py,
# tc_spindle_figures.py, and the neuron/ port).

numpy>=2.0        # np.trapezoid is used (numpy 2.0+)
scipy>=1.10
matplotlib>=3.6
pyyaml>=6.0

# ==============================================================================
#  NEUROSIMULATORS -- all THREE are REQUIRED (used across the project)
# ==============================================================================
# The two that are on PyPI are listed as active requirements below; NEST is not
# on PyPI and must be installed separately (see its note). Heads-up: these are
# large packages with their own dependency trees and can conflict in a single
# environment (TVB in particular has historically pinned older numpy/scipy). If
# `pip install -r requirements.txt` fails to resolve, install each simulator in
# its OWN virtual environment (the NEURON port already uses a dedicated venv --
# see neuron/README.md).

neuron>=9          # NEURON -- multi-compartment / Hodgkin-Huxley (neuron/ port)
                   #   Docs: https://nrn.readthedocs.io

tvb-library        # The Virtual Brain -- large-scale / whole-brain simulator
tvb-data           #   (neural-mass / field level). Docs: thevirtualbrain.org

# NEST 3.x  --  REQUIRED, but NOT installable by pip (not on PyPI). Install via
#   conda-forge or from source, then it is importable as `nest`:
#       conda install -c conda-forge nest-simulator
#   Docs: https://nest-simulator.readthedocs.io/en/stable/installation/
#   Tested with NEST 3.9.  Verify with:  python -c "import nest"
